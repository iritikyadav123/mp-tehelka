
import {atom, selector} from 'recoil'

export const  userInputAtom = atom({
     key : "userInputAtom",
     default : {}
})

export const filterComponent = atom({
     key : "filterComponent",
     default : "non"
})

